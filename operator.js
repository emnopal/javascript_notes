// operator assignment
// +=, -=, *=, /=, %=

// operator comparison
// >, <, <=, >=, !=, == (without datatype comparison), === (with datatype comparison)

// operator logical
// &&, ||, !

// in JS you can use ** operator as power
